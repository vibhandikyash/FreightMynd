---
import IntegrationPage from '../../layouts/IntegrationPage.astro';
import { integrations } from '../../data/integrations';
const integration = integrations.find(i => i.slug === 'cargowise')!;
---
<IntegrationPage integration={integration} />
