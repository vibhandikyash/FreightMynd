---
import SolutionPage from '../../layouts/SolutionPage.astro';
import { solutions } from '../../data/solutions';

const solution = solutions.find(s => s.slug === 'smart-invoice-processing')!;
---

<SolutionPage solution={solution} heroImage="/images/smart-invoice-processing.webp" heroImageAlt="Smart invoice processing system extracting structured data from freight invoices" />
