---
import SolutionPage from '../../layouts/SolutionPage.astro';
import { solutions } from '../../data/solutions';

const solution = solutions.find(s => s.slug === 'smart-invoice-processing')!;
---

<SolutionPage solution={solution} heroImage="/images/smart-invoice-processing-ui-theme.png" heroImageAlt="Smart AI invoice processing system with document upload, extracted invoice table, and export actions" />
