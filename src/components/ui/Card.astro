---
interface Props {
  class?: string;
  href?: string;
}

const { class: className = '', href } = Astro.props;
---

{href ? (
  <a href={href} class={`card block ${className}`.trim()}>
    <slot />
  </a>
) : (
  <div class={`card ${className}`.trim()}>
    <slot />
  </div>
)}
